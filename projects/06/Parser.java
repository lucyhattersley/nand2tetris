class Parser {
    void initialize() {
        // Puts together input stream
        String path = "add/Add.asm";
        String content = Files.readString(path);
    }
    boolean hasMoreCommands() {
        // Returns true/false if input has more commands
        return false;

    }
}